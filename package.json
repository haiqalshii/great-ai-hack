{
  "name": "nebula-ai",
  "version": "1.0.0",
  "description": "AI-powered content generation platform for SMEs",
  "main": "server.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "nodemon server.js",
    "client": "cd client && npm start",
    "install-client": "cd client && npm install"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.891.0",
    "@aws-sdk/client-s3": "^3.892.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.0",
    "multer": "^1.4.5-lts.1",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.0.0",
    "nodemon": "^3.0.0"
  }
}
